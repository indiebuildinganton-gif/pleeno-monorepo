{
  "version": 2,
  "buildCommand": "turbo run build",
  "devCommand": "turbo run dev --parallel",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["iad1"],
  "github": {
    "silent": true
  }
}
