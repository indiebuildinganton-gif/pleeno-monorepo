{
  "buildCommand": "pnpm install --frozen-lockfile --ignore-scripts && pnpm exec turbo build --filter=dashboard",
  "installCommand": "echo 'Install handled in buildCommand'",
  "framework": "nextjs",
  "outputDirectory": "apps/dashboard/.next"
}
