{
  "buildCommand": "pnpm turbo run build --filter=shell",
  "outputDirectory": "apps/shell/.next",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "nextjs",
  "regions": ["iad1"],
  "functions": {
    "apps/shell/app/api/auth/[...nextauth]/route.ts": {
      "maxDuration": 10
    }
  }
}