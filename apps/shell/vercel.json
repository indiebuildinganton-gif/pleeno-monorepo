{
  "buildCommand": "cd ../.. && pnpm install --frozen-lockfile --ignore-scripts && cd apps/shell && pnpm exec next build",
  "installCommand": "echo 'Install handled in buildCommand'",
  "outputDirectory": ".next"
}
