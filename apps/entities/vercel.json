{
  "buildCommand": "cd ../.. && pnpm install --ignore-scripts && cd apps/entities && next build",
  "installCommand": "pnpm install --frozen-lockfile --ignore-scripts",
  "framework": "nextjs",
  "outputDirectory": ".next"
}
