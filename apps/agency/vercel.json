{
  "buildCommand": "pnpm install --frozen-lockfile --ignore-scripts && cd apps/agency && pnpm exec next build",
  "installCommand": "echo 'Install handled in buildCommand'",
  "framework": null,
  "outputDirectory": "apps/agency/.next"
}
