{
  "buildCommand": "pnpm install --frozen-lockfile --ignore-scripts && pnpm exec turbo build --filter=payments",
  "installCommand": "echo 'Install handled in buildCommand'",
  "framework": "nextjs",
  "outputDirectory": "apps/payments/.next"
}
