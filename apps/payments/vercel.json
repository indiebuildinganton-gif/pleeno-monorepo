{
  "buildCommand": "cd ../.. && pnpm install --frozen-lockfile --ignore-scripts && pnpm exec next build --filter=payments",
  "installCommand": "echo 'Install handled in buildCommand'",
  "outputDirectory": ".next"
}
