{
  "buildCommand": "cd ../.. && pnpm install --ignore-scripts && cd apps/reports && next build",
  "installCommand": "pnpm install --frozen-lockfile --ignore-scripts",
  "framework": "nextjs",
  "outputDirectory": ".next"
}
