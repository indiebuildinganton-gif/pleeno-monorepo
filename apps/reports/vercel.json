{
  "buildCommand": "cd ../.. && pnpm install --frozen-lockfile --ignore-scripts && pnpm exec next build --filter=reports",
  "installCommand": "echo 'Install handled in buildCommand'",
  "outputDirectory": ".next"
}
