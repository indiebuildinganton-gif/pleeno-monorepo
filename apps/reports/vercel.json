{
  "buildCommand": "pnpm install --frozen-lockfile --ignore-scripts && pnpm exec turbo build --filter=reports",
  "installCommand": "echo 'Install handled in buildCommand'",
  "framework": "nextjs",
  "outputDirectory": "apps/reports/.next"
}
