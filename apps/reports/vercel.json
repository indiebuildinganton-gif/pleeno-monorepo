{
  "buildCommand": "cd ../.. && pnpm install --frozen-lockfile --ignore-scripts && cd apps/reports && pnpm exec next build",
  "installCommand": "echo 'Install handled in buildCommand'",
  "outputDirectory": ".next"
}
