# Development environment variables for local testing
# Use with /etc/hosts configuration for subdomain testing

# Cookie domain for cross-subdomain authentication in development
NEXT_PUBLIC_COOKIE_DOMAIN=.localhost

# Shell app (authentication service) URL
NEXT_PUBLIC_SHELL_URL=http://shell.localhost:3000
NEXT_PUBLIC_APP_URL=http://shell.localhost:3000

# Individual app URLs (for local subdomain testing)
NEXT_PUBLIC_DASHBOARD_URL=http://dashboard.localhost:3002
NEXT_PUBLIC_AGENCY_URL=http://agency.localhost:3004
NEXT_PUBLIC_ENTITIES_URL=http://entities.localhost:3001
NEXT_PUBLIC_PAYMENTS_URL=http://payments.localhost:3003
NEXT_PUBLIC_REPORTS_URL=http://reports.localhost:3005

# Node environment
NODE_ENV=development

# Copy your development Supabase and other keys here
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
# SUPABASE_SERVICE_ROLE_KEY=
# DATABASE_URL=
# RESEND_API_KEY=
# OPENAI_API_KEY=